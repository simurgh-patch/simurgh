# simurgh 协作规则

- 本项目完全独立。所有实现、样例和测试工程放在本目录；不得修改旁边的 OA 管理端或 oa-app。
- 开始工作先读 README.md、PROGRESS.md、docs/PLAN.md 与 toolchain.lock.json。
- 按 M0 → M1 → M2 的技术关卡推进。核心运行时和性能门槛通过前，不开发完整发布后台来代替运行时结果。
- 普通 Flutter 业务代码零注解、零手工桥接；冷启动激活；Android/iOS ARM64；原型即要求 AOT 与解释执行混合路径。
- 不把官方 SDK 构建、宿主测试、模拟器测试或合成数据称作自研热更新或真机验收。
- 自研运行时不存在时，发布及补丁命令必须非零退出，不能产生伪补丁、模拟成功或普通整包冒充补丁。
- 使用固定上游源码 revision，不改用户日常 Flutter SDK；源码准备默认检查 200 GiB 空间预算。用户于 2026-09-20 明确授权忽略空间检查直接尝试编译；可使用 --ignore-space-check，仍须保留版本与目录保护。
- 依赖锁文件纳入版本控制；不提交构建产物、设备 UDID、签名身份、令牌和私钥。
- 实现变化执行 scripts/verify.sh；真实构建与设备性能独立记录，不能互相替代。
- PROGRESS.md 使用北京时间，记录真实完成项、验证、失败与下一步；至少任务结束及重大阻塞时更新。
- 用户于2026-09-23授权：每轮完成并验证后编写commit message，正文包含 `Important Changes`，提交并推送到 `https://github.com/simurgh-patch/simurgh.git` 的 `main` 分支；保留远程历史，不强制推送。
- 当前阶段不发布包、不提交商店；代码提交/推送不代表运行时、真机或生产门禁通过。
